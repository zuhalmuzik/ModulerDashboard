4. Inventory/Stock Analysis (orta öncelik)
renderInventoryDashboard() — envanter gösterimi (~20 satır)
filterStockAnalysis() — stok analiz filtreleme (~102 satır)
renderStockAnalysisTable() — stok tablosu (~55 satır)
clearStockFilters() — stok filtrelerini temizleme (~10 satır)
renderInventoryCharts() — envanter grafikleri (~131 satır)
renderInventoryTable() — envanter tablosu (~21 satır)
filterInventoryTable() — envanter tablo filtreleme (~43 satır)
getFilteredInventoryData() — filtrelenmiş envanter verisi (~49 satır)
populateInventoryFilters() — envanter filtrelerini doldurma (~144 satır)
switchInventoryAnalysis() — analiz türü değiştirme (~35 satır)
performPriceAnalysis() — fiyat analizi (~55 satır)
performStockAnalysis() — stok analizi (~45 satır)
performPerformanceAnalysis() — performans analizi (~42 satır)
performTrendAnalysis() — trend analizi (~42 satır)
performAlertAnalysis() — uyarı analizi (~44 satır)
Önerilen: js/modules/inventory.js
5. Time Analysis (orta öncelik)
analyzeTime() — zaman analizi (~34 satır)
updateTimeSummary() — zaman özeti güncelleme (~64 satır)
renderHourlyChart() — saatlik grafik (~69 satır)
renderMonthlyTrendChart() — aylık trend grafiği (~67 satır)
renderYearlyTrendChart() — yıllık trend grafiği (~62 satır)
populateTimeFilters() — zaman filtrelerini doldurma (~38 satır)
clearTimeFilters() — zaman filtrelerini temizleme (~11 satır)
renderStoreTimeChart() — mağaza zaman grafiği (~55 satır)
renderCategoryTimeChart() — kategori zaman grafiği (~55 satır)
renderSalesPersonTimeChart() — satış temsilcisi zaman grafiği (~55 satır)
performTimeAIAnalysis() — zaman AI analizi (~234 satır)
Not: js/time-analysis-enhanced.js var; inline fonksiyonları buraya taşımak gerekir.
Önerilen: js/modules/time-analysis.js
6. Product Analysis (düşük öncelik)
searchProduct() — ürün arama (~112 satır)
renderProductStoreChart() — ürün mağaza grafiği (~74 satır)
renderProductCityChart() — ürün şehir grafiği (~74 satır)
renderProductDistrictChart() — ürün ilçe grafiği (~74 satır)
renderProductSalesPersonChart() — ürün satış temsilcisi grafiği (~74 satır)
renderProductMonthlyChart() — ürün aylık grafik (~87 satır)
renderProductDetailTable() — ürün detay tablosu (~170 satır)
performProductAIAnalysis() — ürün AI analizi (~341 satır)
showProductCategoryDropdown() — kategori dropdown göster (~5 satır)
hideProductCategoryDropdown() — kategori dropdown gizle (~4 satır)
filterProductCategories() — kategori filtreleme (~40 satır)
selectProductCategory() — kategori seçimi (~7 satır)
clearProductDateFilters() — ürün tarih filtrelerini temizleme (~6 satır)
Önerilen: js/modules/product-analysis.js